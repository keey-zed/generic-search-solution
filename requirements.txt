# Minimal, pinned-range dependencies for the generic search factory
# (app/core, app/api, app/custom). This mirrors the [project.dependencies]
# in pyproject.toml; prefer `pip install .` / `uv sync` over this file
# when possible.
#
# The legacy artisan Flask app (app/data_loader.py, app/semantic_engine.py,
# app/services.py, app/routes.py, run.py) needs a much heavier stack
# (flask, faiss, sentence-transformers, torch, PyMuPDF, ...). That app is
# not part of the generic core and is intentionally not listed here.
# See pyproject.toml's `legacy-app` optional dependency group instead.

pydantic>=2.0
pyyaml>=6.0
jsonschema>=4.0
rapidfuzz>=3.0   # only needed for app/custom/books fuzzy title filter
flask>=3.0       # only needed for app/api/http.py (exposing SearchEngine over HTTP)
flask-cors>=4.0  # only needed for app/api/http.py's CORS support
pytest>=8.0      # for running tests/
